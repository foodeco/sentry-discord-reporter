# 프로젝트별 Sentry 분석·문서화 흐름

## 목표

Sentry 오류를 프로젝트 단위로 모아 노이즈를 제외하고, 관측 사실·커밋 소스 근거·추정 원인·권장 조치를 Markdown과 Discord로 제공한다. 프로젝트 코드 수정, 이슈 생성, 브랜치·커밋·PR은 이 자동화의 범위가 아니다.

## 프로젝트 매핑

`targets.json`의 `sourceRoots`가 Sentry 프로젝트를 로컬 저장소에 연결한다.

| Sentry 프로젝트 | 로컬 저장소 |
|---|---|
| `weing-dev/orta-client` | `../orta` |
| `weing-dev/orta-cms` | `../orta-cms` |
| `weing-lloyd/dore_demo` | `../dore` |

```json
{
  "sourceRoots": {
    "weing-dev/orta-client": "../orta",
    "weing-dev/orta-cms": "../orta-cms",
    "weing-lloyd/dore_demo": "../dore"
  }
}
```

경로는 `sentry-discord-reporter` 기준이다. 매핑이 없거나 저장소를 읽지 못하면 Sentry 이벤트만으로 분석하고 문서에 분석 방식을 표시한다.

## 1회 실행 흐름

1. **조회 구간 결정**
   - 예약: 09:00, 20:00 KST 사이 구간
   - 수동: 최근 24시간 또는 `LOOKBACK_HOURS`
2. **Sentry 수집**
   - 조직별 토큰으로 설정 프로젝트의 이슈와 최신 이벤트 조회
   - 예외 체인, 애플리케이션 프레임, 허용된 태그만 추출
3. **선별**
   - 해결·무시 상태, 제외 레벨, `ignoreContains`, `ignoredIssueIds` 적용
   - 신규·재발·빈도·영향 사용자 기준으로 최대 `MAX_ISSUES_PER_RUN`건 선택
4. **프로젝트 분리**
   - `organization/project`별로 이슈를 묶음
   - 오류가 없는 설정 프로젝트도 0건 문서를 생성
5. **소스 근거 추출**
   - Sentry 스택의 파일 경로·줄 번호·심볼을 정규화
   - 연결된 저장소의 현재 `HEAD`에서 일치하는 추적 파일 구간만 `git show`로 읽음
   - 경로가 없으면 의미 있는 심볼을 Git에서 검색
   - 코드 구간을 마스킹하고 이슈 키와 함께 분석 입력에 포함
   - 미커밋·미추적 파일과 원본 작업 트리는 읽거나 수정하지 않음
6. **Codex 분석**
   - Sentry 이벤트와 추출된 소스 구간만 전달
   - P0~P3, 노이즈, 판단 근거, 추정 원인, 권장 조치를 구조화된 JSON으로 반환
   - 소스 근거 사용 여부가 확인되지 않으면 Sentry 전용 분석 또는 규칙 기반 분석으로 폴백
7. **프로젝트 문서 생성**
   - 전체 인덱스: 프로젝트별 보고 수, P0~P3, 노이즈, 조회 구간 이벤트, 요약
   - 상세 문서: 우선순위 표, 이슈별 관측·원인·조치·Sentry 링크, 제외 노이즈
8. **Discord 알림**
   - 프로젝트 헤더 아래 우선순위별 이슈를 묶어 전송
   - Discord 제한에 맞춰 여러 메시지로 자동 분할
9. **보관**
   - 로컬은 `reports/`에 생성하되 Git에서는 제외
   - GitHub Actions는 Markdown을 14일 아티팩트로 보관

## 생성 파일

```text
reports/
  2026-08-22/
    0900/
      index.md
      weing-dev__orta-client.md
      weing-dev__orta-cms.md
      weing-lloyd__dore_demo.md
```

## 분석 우선순위

| 등급 | 기준 |
|---|---|
| `P0` | 전체 장애, 데이터 손실, 보안 사고 |
| `P1` | 신규·재발 또는 반복되는 고영향 오류 |
| `P2` | 일반 운영 오류 |
| `P3` | 영향이 낮은 오류 |

네트워크·브라우저 확장·봇·알려진 무해 오류라는 근거가 충분할 때만 노이즈로 제외한다. 근거가 부족한 원인은 단정하지 않고 필요한 로그·지표·재현 방법을 조치에 기록한다.

## 실행 환경 경계

### 로컬 Windows

- 로그인된 ChatGPT 계정의 Codex CLI 사용
- 연결된 세 저장소의 커밋 소스 근거 사용
- 비밀 환경변수는 Codex 자식 프로세스에 전달하지 않음
- Windows 작업 스케줄러 등록은 별도 작업

### GitHub Actions

- 로컬 ChatGPT 로그인과 형제 저장소를 사용할 수 없음
- 기본적으로 OpenAI API 또는 규칙 기반 분석
- 소스 기반 분석에는 대상 private 저장소 checkout 권한과 별도 구성이 필요
- 생성 문서는 `sentry-reports-<run_id>` 아티팩트로 14일 보관

## 현재 의도적으로 제외한 것

- 이슈 군집 캐시와 DB
- Sentry release SHA와 Git 커밋의 자동 매칭
- HTML 대시보드와 장기 추세 저장

현재 Markdown과 프로젝트 요약으로 운영이 가능하다. 같은 이슈의 반복 분석 비용이나 장기 추세 요구가 실제로 생길 때 캐시 또는 저장소를 추가한다.

## 완료 조건

- 설정된 모든 프로젝트가 인덱스에 표시된다.
- 보고 이슈마다 관측 사실, 원인, 조치, Sentry 링크가 있다.
- 가능한 경우 분석에 실제 `HEAD` 파일 경로와 심볼이 포함된다.
- 노이즈는 근거와 함께 별도 표에 기록된다.
- 소스 또는 AI 분석 실패 시 전체 실행이 중단되지 않는다.
- 대상 소스 저장소와 사용자 작업 트리를 변경하지 않는다.
