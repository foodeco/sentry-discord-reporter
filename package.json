{
  "name": "sentry-discord-reporter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env report.mjs",
    "test:unit": "node --test"
  }
}
